export const COLUMNS_COUNT = 30;
export const MAX_NUMBER = COLUMNS_COUNT;
export const SORT_INTERVAL_MS = 1000;

export enum MESSAGES {
  SOLVED = `Sorting is finished!`,
  NOT_SOLVED = `Not sorted`,
}
